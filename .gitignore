# Sous-dossiers à ne pas tracker
(ancien) résultats et comparaison/
.vs/
2026_1205 résultats et comparaisons/
2026_3004 résultats et comparaisons/
__pycache__/
ac_copies/
archive claude code/
archive/
claude's plans/
claude_fix/
debug_resultats_comparaison/
output/
ressources code/
ressources pdf/
resume session a relire/
version du ac_pure_flexion avant le 27_05/
visu obtenues/
visu_ana/
word de resultat et comparaison/

# Fichiers output à la racine
output_*.txt
C：tmpform_out.txt

# Notes de session Claude a la racine (jetables) -- PAS toute la doc :
# la regle '*.md' qui existait ici rendait le depot impossible a documenter
# (ni README, ni doc de tests, ni plan).
resume_session*.md
global_resume_session*.md
mix_fonctionnement_global.md

# Caches d'outils
.pytest_cache/

# Script genere par tools/run_comparatif.py
_run_comparatif.py

# Journaux de baseline : un par execution de tools/baseline_run.py, que
# la documentation demande de rejouer a chaque commit. Le depot suit la
# REFERENCE et le plancher de bruit ; les runs, eux, sont jetables et
# salissaient `git status` -- ce qui invite au `git add -A` que
# CONTRIBUTING interdit.
baselines/*/run_*.jsonl
